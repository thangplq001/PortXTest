name: Run Datadog Synthetic Tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  synthetics:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18'

      - name: Install Datadog CLI
        run: npm install -g @datadog/datadog-ci

      - name: Run Datadog Synthetic Tests
        env:
          DATADOG_API_KEY: '12883751f7332e233ae9186cc2ff6f65'
          DATADOG_APP_KEY: 'e8dd9aa4ca8cc33b01eac786d0e832c890c6efaa'
          LOGIN_USER: 'autotest'
          LOGIN_PASSWORD: 'G8d2pass@'
        run: datadog-ci synthetics run-tests --apiKey $DATADOG_API_KEY --appKey $DATADOG_APP_KEY --public-id wz4-fvd-ka9 --datadogSite ap1.datadoghq.com --variable "LOGIN_USER=$LOGIN_USER" --variable "LOGIN_PASSWORD=$LOGIN_PASSWORD"
      
